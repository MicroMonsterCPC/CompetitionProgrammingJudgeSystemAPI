version: '2'
services:
  db:
    image: postgres
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    environment:
      - GITHUB_KEY
      - GITHUB_SECRET
    volumes:
      - .:/myapp
    ports:
      - "3000:3000"
    depends_on:
      - db

